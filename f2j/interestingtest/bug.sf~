data PolyList[A] = Nil
			| 	Cons A (PolyList[A]);

--type CharList = PolyList[Int];

let rec readPolyList(s: String): PolyList[Int] =
	if s.length() == 0 then (Nil[Int])
	else (Cons[Int] 1 (readPolyList("")));



--readTestCases("")
readPolyList "hello"
